{
  "name": "stupid-radar-chart",
  "version": "1.0.0",
  "description": "Stupid simple radar chart generator with transparent PNG export",
  "main": "index.html",
  "scripts": {
    "start": "cd ../backend && python3 -m uvicorn main:app --host 0.0.0.0 --port 8000 --reload & cd ../frontend && python3 -m http.server 8080",
    "backend": "cd ../backend && python3 -m uvicorn main:app --host 0.0.0.0 --port 8000 --reload",
    "frontend": "cd ../frontend && python3 -m http.server 8080"
  },
  "keywords": ["radar", "chart", "visualization"],
  "author": "Andrea Carmisciano",
  "license": "MIT"
}
